<view class="page">

    <!-- map.wxml -->
    <map id="map" longitude="{{pos[0]}}" latitude="{{pos[1]}}" scale="14" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}" bindregionchange="regionchange" show-location style="width: 100%; height:2000rpx;"></map>

    <loading hidden="{{!loading}}" bindchange="loadingChange">
        加载中...
    </loading>
</view>

